pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project'
            }
        }
        
        stage('Run Tests') {
            steps {
                echo "testing"
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the application'
            }
        }

        stage('Hello Docker') {  
            steps {
                echo 'hello docker'
            }
        }
    }
}
